%h1 New Project
.row
  .span6.offset4
    = form_for @project, url: user_projects_path do |f|
      = f.label :name
      = f.text_field :name
      %p #{f.submit "Create", class: "btn btn-medium btn-primary"}